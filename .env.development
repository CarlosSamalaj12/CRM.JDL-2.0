# Override para desarrollo local: el front (localhost:5173) habla con el backend local
# que está corriendo en el puerto de APP_PORT. El proxy de Vite (vite.config.js)
# se encarga de rutear /api/* → http://127.0.0.1:3000 cuando VITE_API_URL queda vacío.
VITE_API_URL=
